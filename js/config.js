/**
 * Supabase client configuration.
 * Replace the placeholders below (or create `config.local.js` exporting the same names
 * and swap the import in `supabase.js` if you prefer not to commit keys).
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
