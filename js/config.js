/**
 * Supabase defaults (safe to commit — placeholders).
 * For real keys on your machine only: copy `config.local.example.js` → `config.local.js`
 * and fill it in. That file is gitignored; `supabase.js` loads it automatically when present.
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
