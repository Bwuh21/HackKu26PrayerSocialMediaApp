/**
 * Option A — edit committed `config.js` in place (not ideal if you push to GitHub).
 * Option B — copy `config.local.example.js` to `config.local.js` and paste values there
 * (recommended: that file stays gitignored).
 * Project Settings → API → Project URL + anon public key
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
